{
  "name": "u7-hub",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "@happy-dom/global-registrator": "^18.0.1",
    "@playwright/test": "^1.55.1",
    "@types/bun": "latest",
    "@types/markdown-it": "^14.1.2",
    "dotenv-cli": "^10.0.0",
    "playwright": "^1.55.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "happy-dom": "^18.0.1",
    "markdown-it": "^14.1.1",
    "markdown-it-attrs": "^4.3.1",
    "markdown-it-bracketed-spans": "^1.0.1",
    "markdown-it-container": "^4.0.0"
  },
  "scripts": {
    "db:backup": "NODE_ENV=production bun run src/app/db-utils/backup-db.ts",
    "db:backup:list": "bun run src/app/db-utils/backup-db.ts --list",
    "db:backup:cleanup": "bun run src/app/db-utils/backup-db.ts --cleanup",
    "db:backup:named": "bun run src/app/db-utils/backup-db.ts --name=pre-migration",
    "db:migrate": "NODE_ENV=production bun run src/app/db-utils/run-migrations.ts --force",
    "db:status": "NODE_ENV=production bun run src/app/db-utils/run-migrations.ts --status",
    "metrics": "bun run src/app/db-utils/view-events.ts",
    "metrics:watch": "bun run src/app/db-utils/view-events.ts --watch",
    "dev:server": "NODE_ENV=development bun --hot run src/app/server.ts",
    "build:dev": "NODE_ENV=development bun run build.ts --dev --watch",
    "start:dev": "bun run build:dev & bun run dev:server",
    "build:prod": "NODE_ENV=production bun run build.ts --prod",
    "start:prod": "bun run db:backup && bun run db:migrate && bun run build:prod && pm2 start ecosystem.config.cjs --env production",
    "test": "bun test",
    "test:ui": "bun test src/ui/**/*.test.ts",
    "test:api": "bun test src/api/**/*.test.ts",
    "test:domain": "bun test src/domain/**/*.test.ts",
    "test:watch": "bun test --watch",
    "test:e2e": "bun test test/e2e/form-submission.test.ts test/e2e/analytics.test.ts test/e2e/browser-form.test.ts test/e2e/chat.test.ts"
  }
}
